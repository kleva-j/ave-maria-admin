{
  "name": "@avm-daily/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "setup:crons": "convex run init:setup",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@avm-daily/application": "workspace:*",
    "@avm-daily/domain": "workspace:*",
    "@convex-dev/aggregate": "^0.2.1",
    "@convex-dev/crons": "^0.2.0",
    "@convex-dev/workos-authkit": "^0.1.6",
    "convex": "catalog:",
    "convex-audit-log": "^0.2.0",
    "date-fns": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@avm-daily/config": "workspace:*",
    "@types/node": "^24.3.0",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
