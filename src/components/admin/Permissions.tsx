import { Table, Tabs } from '@mantine/core';

export const Permissions = () => {
  return (
    <Tabs.Panel value="permissions" pt="xs">
      Permissions
      <Table>
        <thead></thead>
        <tbody></tbody>
      </Table>
    </Tabs.Panel>
  );
};
