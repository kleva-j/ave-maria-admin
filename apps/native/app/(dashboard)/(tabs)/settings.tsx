import { TabPlaceholder } from "@/components/user-shell/tab-placeholder";

export default function SettingsTab() {
  return (
    <TabPlaceholder
      title="Settings"
      copy="Bank accounts, notifications, and more."
      pr="PR N05 + N06"
      icon="settings"
    />
  );
}
